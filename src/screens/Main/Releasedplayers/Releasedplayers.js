import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Releasedplayers = () => {
  return (
    <View>
      <Text>Releasedplayers</Text>
    </View>
  )
}

export default Releasedplayers

const styles = StyleSheet.create({})